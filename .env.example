# .env.example
# ==========================================
# JX CLOUD 生产环境配置模板
# ==========================================

# 数据库连接 (Supabase Connection Pooler / Transaction Mode)
# 端口 6543 适用于 Serverless/Edge 环境，推荐使用
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@[YOUR-PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true&sslmode=require

# Supabase 公开配置 (前端使用)
VITE_SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
VITE_SUPABASE_ANON_KEY=[YOUR-ANON-KEY]

# Better-Auth 核心配置
# 生成方式: openssl rand -base64 32
BETTER_AUTH_SECRET=[GENERATED-32-CHAR-STRING]
BETTER_AUTH_URL=https://your-domain.com

# 服务端特权密钥 (仅后端/Edge Functions 使用，严禁泄露)
SUPABASE_SERVICE_ROLE_KEY=[YOUR-SERVICE-ROLE-KEY]

# Supabase 项目标识符 (用于部署脚本)
SUPABASE_PROJECT_REF=[YOUR-PROJECT-REF]

# Vercel 部署配置
VERCEL_PROJECT_ID=[YOUR-VERCEL-PROJECT-ID]
VERCEL_ORG_ID=[YOUR-VERCEL-ORG-ID]