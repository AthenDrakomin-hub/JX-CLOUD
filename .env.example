# ============================================================
# 🏨 江西云厨 - 环境变量配置模板 (生产环境必需)
# ============================================================

# 1. Supabase 配置
# VITE_ 前缀的变量会被 Vite 注入到前端，用于客户端访问
VITE_SUPABASE_URL="https://your-project.supabase.co"           # 替换为你的 Supabase 项目 URL
VITE_SUPABASE_ANON_KEY="your-anon-key"                        # 替换为你的 Supabase Anon Key

# 2. 数据库连接配置 (服务端专用)
# DATABASE_URL 使用 Supabase 连接池端口 6543 (推荐用于生产环境)
DATABASE_URL="postgresql://your-db-url:6543/postgres?pgbouncer=true"
POSTGRES_URL="postgresql://your-db-url:6543/postgres?pgbouncer=true"

# DIRECT_URL 用于 Schema 迁移 (直连端口 5432)
DIRECT_URL="postgresql://your-db-url:5432/postgres"

# 3. Better Auth 配置
# 生产环境必须使用强密钥 (至少32字符随机字符串)
BETTER_AUTH_SECRET="your-production-secret-here-change-this"   # 必须替换为安全密钥
BETTER_AUTH_URL="https://yourdomain.com"                      # 替换为你的生产域名
VITE_BETTER_AUTH_URL="https://yourdomain.com"                 # 与上面保持一致

# 4. 可选配置
# Redis 连接 (如果使用缓存)
# REDIS_URL="redis://localhost:6379"

# SMTP 配置 (如果需要邮件功能)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"