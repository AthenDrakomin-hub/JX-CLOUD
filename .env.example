# ============================================================
# 🏨 酒店管理系统 - 环境变量模板 (Environment Template)
# ============================================================
# 说明：本地开发请复制此文件并重命名为 .env，然后填入真实数值。
# 生产环境：已关联 Vercel + Supabase Integration 自动注入。
# ============================================================

# 1. Supabase API (前端/Edge Functions)
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# 2. 数据库连接 (后端 Drizzle ORM 专用)
# DATABASE_URL 用于业务运行 (建议使用端口 6543 + pgbouncer=true)
DATABASE_URL="postgresql://postgres:password@db.your-id.supabase.co:6543/postgres?pgbouncer=true&sslmode=require"
POSTGRES_URL="postgresql://postgres:password@db.your-id.supabase.co:6543/postgres?pgbouncer=true&sslmode=require"

# DIRECT_URL 用于数据库迁移 (必须使用端口 5432 直连)
DIRECT_URL="postgresql://postgres:password@db.your-id.supabase.co:5432/postgres?sslmode=require"

# 3. Better Auth (认证服务配置)
BETTER_AUTH_SECRET="random_32_chars_secret"
BETTER_AUTH_URL="http://localhost:3001"
VITE_BETTER_AUTH_URL="http://localhost:3001"

# 4. AI 与 外部服务
# 原 .env.local 中的变量归并至此
GEMINI_API_KEY="your-gemini-api-key"

# 5. 业务硬件配置
PRINTER_IP="192.168.1.100"
WEBHOOK_SECRET="whsec_your_key"